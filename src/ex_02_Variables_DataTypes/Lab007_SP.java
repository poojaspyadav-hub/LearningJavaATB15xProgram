package ex_02_Variables_DataTypes;

import org.w3c.dom.ls.LSOutput;

public class Lab007_SP {
    //   Exit code - In Java, exit codes are integers returned to the operating system when a program terminates.
    //   Java programs can explicitly terminate using System.exit(int status)--System.exit(0)
    //  0-Normal termination, 1-General error / abnormal termination, 2-Misuse of shell built‑ins (sometimes used in scripts), >0-Application‑specific error codes(could mean a custom error defined by the developer)

/*
keywords are reserved words with predefined meanings (like class, int, if) that cannot be used as names,
while identifiers are user-defined names for classes, variables, methods, etc.,
which must follow specific rules (start with a letter, no spaces, not a keyword, case-sensitive)
 */
public static void main(String[] args) {
    System.out.println("Lab007");

}


}
